# /etc/exports – managed by the NFS role
# Generated by Ansible

{% for host in groups[nfs_client_group] %}
{{ nfs_export_path }} {{ hostvars[host]['ansible_host'] | default(host) }}({{ nfs_export_options }})
{% endfor %}

